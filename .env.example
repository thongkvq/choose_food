# Chép file này thành .env rồi điền khoá thật. .env đã nằm trong .gitignore — KHÔNG commit.
# Google Places API (New) — lấy ở https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_KEY=

# (tuỳ chọn) đổi cổng
# PORT=4321
# HTTPS_PORT=8443
